export enum ConfigType {
    String = 0,
    Integer = 1,
    Boolean = 2,
    Array = 3
}

export enum Gender {
    Male = 0,
    Female = 1,
    Other = 2
}

export enum StageType {
    Flat = 0,
    Moutain = 1,
    IntermediateSprint = 2
}
